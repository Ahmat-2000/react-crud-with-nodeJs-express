import React from "react";
export default function NoPage(params) {
    return(
        <div className="container content home">
            <h1>No page or Disconnected</h1>
            <p>Please make sure to be connected or enter a valid route</p>
        </div>
    );
}